import { readData, writeData } from './firebase';

export async function fetchAllUsers() {
    // Se in futuro userai un “/profiles” pubblico, potrai leggerlo qui.
    // Per ora ritorna oggetto vuoto per non rompere chiamate esistenti.
    return {};
}
